# Copy to .env (or export directly) to point the app at a different Postgres
# instance -- e.g. a hosted one (RDS, Supabase, etc.) instead of local dev.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/astro_dev
